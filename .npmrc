# React Native / Expo (Metro, Babel and native modules) assume a flat
# node_modules layout. pnpm's default isolated store breaks module
# resolution for RN, so use a hoisted linker as recommended by Expo.
# https://docs.expo.dev/guides/monorepos/#pnpm
node-linker=hoisted
